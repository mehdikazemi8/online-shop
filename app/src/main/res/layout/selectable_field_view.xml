<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/rounded_edittext"
    android:minHeight="@dimen/app_fields_height"
    android:foreground="?android:attr/selectableItemBackground"
    android:id="@+id/cs_container"
    >

    <com.veevapp.customer.view.customwidget.AppTextView
        android:id="@+id/tv_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_margin="@dimen/space_small"
        android:text="رنگ را انتخاب کنید"
        android:gravity="center"
        android:textColor="@color/fields_text_unselected"
        android:fontFamily="@font/iransans"
        android:textSize="@dimen/text_size_normal"
        />



    <android.support.v7.widget.AppCompatImageView
        android:id="@+id/iv_dropdown"
        android:layout_width="@dimen/app_fields_height"
        android:layout_height="@dimen/app_fields_height"
        app:srcCompat="@drawable/ic_arrow_drop_down_black_24dp"
        android:padding="8dp"
        android:tint="@color/colorAccent"
        />

</android.support.constraint.ConstraintLayout>